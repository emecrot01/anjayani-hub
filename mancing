-- Booting Rayfield UI Library
local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

-- Notif Startup
Rayfield:Notify({
    Title = "Sabar Bngst 🗿🖕🏻",
    Content = "Lagi Loading",
    Duration = 3
})

-- Window Utama
local Window = Rayfield:CreateWindow({
    Name = "EmeCRT Fish It 🐟",
    LoadingTitle = "Fish It 🐟",
    LoadingSubtitle = "Mata Pancing 🥵YAMETEHH🥵",
    ConfigurationSaving = { Enabled = false }
})

-- ====== TAB 1: Home 🏠 ======
local TabHome = Window:CreateTab("🏠 Home")
TabHome:CreateLabel("Fish It🐟 v2")
TabHome:CreateSection("📜 Changelog")
TabHome:CreateParagraph({
    Title = "Fish It🐟 v2",
    Content = [[
📢 Fitur Yang Tersedia:
- Auto Fishing 🎣
- Auto Freeze Area Farm (🐙,💰,🔮)
- Auto Sell 💰
- Teleport 🏝️
- Weather Shop 🏪🔆
- Utility ⚙️

🔔 Fitur Baru
- Freeze System Di Beberapa Area Grinding
- Auto Equip Rod Saat Auto Fishing Dinyalakan 

🔀 Changed
- Ada Beberapa Pembaruan Untuk Fitur Fitur Script Dan Memperbaiki Beberapa BUG
    ]]
})
TabHome:CreateSection("👥 Credits")
TabHome:CreateParagraph({
    Title = "Developer",
    Content = "EmeCRT"
})

-- ===== TAB 2: Auto Fishing 🎣 =====
local TabFishing = Window:CreateTab("🎣 Auto Fishing")
local Net = game:GetService("ReplicatedStorage").Packages["_Index"]["sleitnick_net@0.2.0"].net

-- 📌 List lokasi grinding / fishing
local grindLocations = {
    {Name = "Enchant Stone Farm🔮", CFrame = CFrame.new(-2107.84, 6.27, 3692.49)},
    {Name = "Money Farm 💰", CFrame = CFrame.new(-647.76, 30.01, 118.15)},
    {Name = "Kraken Farm 🐙", CFrame = CFrame.new(-3730.24, -135.07, -886.17)},
}

local selectedTP = nil

-- Section: Specific TP Area
TabFishing:CreateSection("Specific TP Area 🗺️")

TabFishing:CreateDropdown({
    Name = "Select TP Area 🧭",
    Options = (function()
        local names = {}
        for _, loc in ipairs(grindLocations) do
            table.insert(names, loc.Name)
        end
        return names
    end)(),
    CurrentOption = {},
    Callback = function(Value)
        local selectedName = Value[1]  -- FIX: Value adalah table
        for _, loc in ipairs(grindLocations) do
            if loc.Name == selectedName then
                selectedTP = loc
                break
            end
        end
        Rayfield:Notify({
            Title = "Area Selected 🧭",
            Content = "Kamu pilih: " .. selectedName,
            Duration = 2
        })
    end
})

-- Specific TP Area Toggle
local freezeConnection
local tpToggleActive = false

TabFishing:CreateToggle({
    Name = "Freeze TP to Selected Area ⚡️",
    CurrentValue = false,
    Flag = "FreezeTP",
    Callback = function(Value)
        tpToggleActive = Value

        if tpToggleActive and selectedTP then
            -- Teleport dulu sekali
            local hrp = game.Players.LocalPlayer.Character:WaitForChild("HumanoidRootPart")
            hrp.CFrame = selectedTP.CFrame

            -- Freeze karakter
            freezeConnection = game:GetService("RunService").Heartbeat:Connect(function()
                if tpToggleActive and hrp then
                    hrp.CFrame = selectedTP.CFrame
                    hrp.Velocity = Vector3.new(0,0,0)
                end
            end)

            Rayfield:Notify({
                Title = "Freeze TP ✅",
                Content = "Kamu dibekukan di " .. selectedTP.Name,
                Duration = 1
            })
        else
            -- Lepas freeze
            if freezeConnection then
                freezeConnection:Disconnect()
                freezeConnection = nil
            end

            Rayfield:Notify({
                Title = "Freeze TP ❌",
                Content = "Freeze dimatikan, karakter bisa gerak lagi",
                Duration = 1
            })
        end
    end
})

-- Section: Automatic Fishing
TabFishing:CreateSection("Automatic Fishing 🎣")

local running = false
local currentFishCount = 0
local SellAllRemote = Net["RF/SellAllItems"]

-- Auto Sell Config
local autoSellActive = false
local targetFishCount = 0

-- Auto Sell Function
local function autoSellCheck()
    if autoSellActive and SellAllRemote and targetFishCount > 0 and currentFishCount >= targetFishCount then
        task.spawn(function()
            local ok, err = pcall(function()
                SellAllRemote:InvokeServer()
            end)
            if ok then
                task.wait(0.3)
                Rayfield:Notify({
                    Title = "Auto Sell 💰",
                    Content = ("Sold %d fishes ✅"):format(currentFishCount),
                    Duration = 2
                })
                currentFishCount = 0
            else
                warn("[Fish It] SellAll failed:", err)
            end
        end)
    end
end

-- Auto Fishing Loop
local function fish()
    task.wait(1)
    while running do
        Net["RF/ChargeFishingRod"]:InvokeServer(tick())
        task.wait(0.01)

        Net["RF/RequestFishingMinigameStarted"]:InvokeServer(-1 + math.random(), 1)
        task.wait(1 + math.random() * 0.2)

        Net["RE/FishingCompleted"]:FireServer()
        task.wait(0.2 + math.random() * 0.1)

        Net["RE/FishingCompleted"]:FireServer()

        currentFishCount += 1
        print("Ikan 🐟: " .. currentFishCount)
        autoSellCheck()

        task.wait(0.2 + math.random() * 0.1)
    end
end

TabFishing:CreateToggle({
    Name = "Auto Fishing 🐟",
    CurrentValue = false,
    Callback = function(Value)
        running = Value
        if running then
            task.spawn(fish)
            Rayfield:Notify({
                Title = "Auto Fishing 🎣",
                Content = "Dimulai...",
                Duration = 2
            })
        else
            Rayfield:Notify({
                Title = "Auto Fishing 🎣",
                Content = "Dihentikan!",
                Duration = 2
            })
        end
    end
})

-- Section + Tutorial
TabFishing:CreateSection("📚 Tutorial 📚")
TabFishing:CreateParagraph({
    Title = "By EmeCRT",
    Content = [[
• TP Farm Area (Khusus TP Saja Ke Area Area Grind)
• Auto Fishing Biasa 🐟 (Default) Tidak Harus Pake Fitur TP
    ]]
})

-- ====== TAB 3: Auto Sell 💰 ======
local TabSell = Window:CreateTab("💰 Auto Sell")
TabSell:CreateToggle({
    Name = "Auto Sell 💰",
    CurrentValue = false,
    Callback = function(Value)
        autoSellActive = Value
        if Value then
            Rayfield:Notify({
                Title = "Auto Sell Activated ✅",
                Content = "Script akan auto sell sesuai target ikan",
                Duration = 1
            })
        else
            Rayfield:Notify({
                Title = "Auto Sell Deactivated ❌",
                Content = "Auto sell dimatikan",
                Duration = 1
            })
        end
    end
})
TabSell:CreateInput({
    Name = "Target Fish Count 🎯",
    PlaceholderText = "Contoh: 100",
    RemoveTextAfterFocusLost = false,
    Callback = function(Text)
        local num = tonumber(Text)
        if num and num > 0 then
            targetFishCount = num
            Rayfield:Notify({
                Title = "Target Updated 🎯",
                Content = ("Target auto sell: %d ikan"):format(targetFishCount),
                Duration = 1
            })
        else
            Rayfield:Notify({
                Title = "Input Error ❌",
                Content = "Masukkan angka valid!",
                Duration = 1
            })
        end
    end
})

-- Section + Tutorial
TabSell:CreateSection("📚 Tutorial 📚")
TabSell:CreateParagraph({
    Title = "By EmeCRT",
    Content = [[
• Set Jumlah Fish Yang Ingin Di Jual
• Aktifkan "Auto Sell 💰"
• Kalau jumlah ikan tercapai → otomatis dijual
    ]]
})

-- ====== TAB 4: Teleport ✈️ ======
local TabTP = Window:CreateTab("✈️ Teleport")

-- Data lokasi teleport (jangan diubah urutannya)
local teleportLocations = {
    { Name = "Stingray Shores 🐠", Pos = Vector3.new(19.035, 15, 2889.627) },
    { Name = "Kohana Island 🏝️", Pos = Vector3.new(-642.76, 19, 618.787) },
    { Name = "Kohana Volcano 🌋", Pos = Vector3.new(-596.92, 64, 105.63) },
    { Name = "Coral Reefs 🐚", Pos = Vector3.new(-2797.356, 15, 2110.986) },
    { Name = "Esoteric Island 🏝️ 🌌", Pos = Vector3.new(2025.875, 30, 1392.24) },
    { Name = "Esoteric Depth 🌌", Pos = Vector3.new(3211.25, -1297, 1412.00) },
    { Name = "Tropical Grove 🌴", Pos = Vector3.new(-2046.46, 13, 3658.995) },
    { Name = "Crater Island ⛰️", Pos = Vector3.new(863.528, 40, 4946.208) },
    { Name = "Weather Machine Island 🕰️", Pos = Vector3.new(-1503.27, 8, 1909.59) },
    { Name = "Lost Isle (Sisyphus Statue) 🔱", Pos = Vector3.new(-3693.872, -132, -1042.617) },
    { Name = "Lost Isle (Treasure Hall) 💎", Pos = Vector3.new(-3600.823, -313, -1405.601) },
    { Name = "Lost Isle (Treasure Room) 🏴‍☠️", Pos = Vector3.new(-3598.215, -280, -1640.74) },
}

-- Lokasi yang dipilih
local selectedLocation = nil

-- Dropdown pilih lokasi
TabTP:CreateDropdown({
    Name = "Pilih Lokasi 📍",
    Options = (function()
        local names = {}
        for _, loc in ipairs(teleportLocations) do
            table.insert(names, loc.Name)
        end
        return names
    end)(),
    CurrentOption = nil,
    MultipleOptions = false,
    Callback = function(Option)
        -- Rayfield Dropdown kasih table, ambil elemen pertama
        if type(Option) == "table" then
            selectedLocation = Option[1]
        else
            selectedLocation = Option
        end
        Rayfield:Notify({
            Title = "Lokasi Dipilih 📍",
            Content = "Lokasi: " .. tostring(selectedLocation),
            Duration = 1
        })
    end
})

-- Tombol Teleport
TabTP:CreateButton({
    Name = "Teleport 🚀",
    Callback = function()
        if not selectedLocation then
            Rayfield:Notify({
                Title = "Error ❌",
                Content = "Belum pilih lokasi!",
                Duration = 1
            })
            return
        end

        local pos = nil
        for _, loc in ipairs(teleportLocations) do
            if loc.Name == selectedLocation then
                pos = loc.Pos
                break
            end
        end

        if pos then
            game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(pos)
            Rayfield:Notify({
                Title = "Teleport ✅",
                Content = "Berhasil teleport ke " .. selectedLocation,
                Duration = 1
            })
        else
            Rayfield:Notify({
                Title = "Gagal ❌",
                Content = "Lokasi tidak ditemukan!",
                Duration = 1
            })
        end
    end
})

-- Section + Tutorial
TabTP:CreateSection("📚 Tutorial 📚")
TabTP:CreateParagraph({
    Title = "By EmeCRT",
    Content = [[
• Tinggal Pilih Mau TP Ke Pulau Mana (gitu doang masa kaga tau🗿🗿🗿)
    ]]
})

-- ====== TAB 5: Shop 🏪 ======
local TabShop = Window:CreateTab("Shop 🏪") 
 
-- ====== SECTION: WEATHER SHOP 🔆 ======
local SectionWeather = TabShop:CreateSection("Weather Shop 🔆")
 
-- ====== HELPER BELI CUACA ======
local function buyWeather(weatherName, displayName)
    local ok, result = pcall(function()
        return game:GetService("ReplicatedStorage")
            :WaitForChild("Packages")
            :WaitForChild("_Index")
            :WaitForChild("sleitnick_net@0.2.0")
            :WaitForChild("net")
            :WaitForChild("RF/PurchaseWeatherEvent")
            :InvokeServer(weatherName)
    end)
 
    if ok and result then
        Rayfield:Notify({
            Title = "Buying Successfully ✅",
            Content = "Thankyou Sultan 👑💰",
            Duration = 1
        })
    else
        Rayfield:Notify({
            Title = "Buying Fail ❌",
            Content = "Cek Udah Beli Atau Duit Kurang, Kalau Kurang. MISKIN GAUSH BELAGU🗿🖕🏻",
            Duration = 1
        })
    end
end
 
-- ====== DATA WEATHER ======
local weathers = {
    { Name = "Wind 💨 ($10K 💰)",      Id = "Wind",       Display = "Wind 💨" },
    { Name = "Cloudy ☁️ ($20K 💰)",   Id = "Cloudy",    Display = "Cloudy ☁️" },
    { Name = "Snow ❄️ ($15K 💰)",     Id = "Snow",      Display = "Snow ❄️" },
    { Name = "Storm ⛈️ ($35K 💰)",    Id = "Storm",     Display = "Storm ⛈️" },
    { Name = "Radiant ✨ ($50K 💰)",   Id = "Radiant",   Display = "Radiant ✨" },
    { Name = "Shark Hunt 🦈 ($300K 💰)", Id = "Shark Hunt", Display = "Shark Hunt 🦈" },
}
 
-- Buat list nama untuk dropdown
local weatherNames = {}
for _, w in ipairs(weathers) do
    table.insert(weatherNames, w.Name)
end
 
-- ====== VARIABEL PILIHAN (default ke Wind) ======
local selectedWeather = weatherNames[1]
 
-- ====== DROPDOWN: SELECT WEATHER 🛒 ======
local DropdownWeather = TabShop:CreateDropdown({
    Name = "Select Weather 🛒",
    Options = weatherNames,
    CurrentOption = weatherNames[1], -- default Wind
    Callback = function(Value)
        if type(Value) == "table" then Value = Value[1] end
        selectedWeather = Value
    end,
})
 
-- (Opsional) Set default ke Wind di UI
pcall(function() DropdownWeather:Set(weatherNames[1]) end)
 
-- ====== BUTTON: BUY WEATHER 💲 ======
TabShop:CreateButton({
    Name = "Buy Weather 💲",
    Callback = function()
        if not selectedWeather then
            Rayfield:Notify({
                Title = "Shop 🏪",
                Content = "Pilih Cuacanya Bre🗿",
                Duration = 1
            })
            return
        end
        for _, w in ipairs(weathers) do
            if w.Name == selectedWeather then
                buyWeather(w.Id, w.Display)
                break
            end
        end
    end,
})
 
-- Section + Tutorial
TabShop:CreateSection("📚 Tutorial 📚")
TabShop:CreateParagraph({
    Title = "By Mammoth👑",
    Content = [[
• Tinggal Pilih Mau Beli Yang Mana, Minimal Jangan Miskin Aja🗿
    ]]
})
 
-- ====== TAB 6: Utility ⚙️ ======
local TabUtility = Window:CreateTab("Utility ⚙️")
 
-- Services
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local VirtualUser = game:GetService("VirtualUser")
 
-- Variabel global
local Humanoid
local defaultWalkSpeed = 20
local defaultJumpPower = 50
local infiniteJump = false
local antiAfkConnection
 
-- Fungsi untuk update Humanoid setiap spawn / respawn
local function setupCharacter(char)
    Humanoid = char:WaitForChild("Humanoid")
 
    -- Update default sesuai server
    defaultWalkSpeed = Humanoid.WalkSpeed
    defaultJumpPower = Humanoid.JumpPower
 
    -- Reset slider ke default (biar balik normal pas mati/reset)
    if walkSpeedSlider then walkSpeedSlider:Set(defaultWalkSpeed) end
    if jumpPowerSlider then jumpPowerSlider:Set(defaultJumpPower) end
end
 
-- Pasang ke char sekarang
if LocalPlayer.Character then
    setupCharacter(LocalPlayer.Character)
end
-- Pasang ke setiap respawn
LocalPlayer.CharacterAdded:Connect(setupCharacter)
 
-- ===== Walk Speed =====
walkSpeedSlider = TabUtility:CreateSlider({
    Name = "Walk Speed 👟⚡️",
    Range = {0, 500}, -- dinaikin max 500
    Increment = 1,
    Suffix = "Speed",
    CurrentValue = defaultWalkSpeed,
    Flag = "WalkSpeed",
    Callback = function(Value)
        if Humanoid then
            Humanoid.WalkSpeed = Value
        end
    end,
})

-- ===== Jump Boost =====
jumpPowerSlider = TabUtility:CreateSlider({
    Name = "Jump Boost 🤸‍♂️🚀",
    Range = {0, 500}, -- dinaikin max 500
    Increment = 5,
    Suffix = "Power",
    CurrentValue = defaultJumpPower,
    Flag = "JumpPower",
    Callback = function(Value)
        if Humanoid then
            Humanoid.JumpPower = Value
        end
    end,
})

-- ===== Infinite Jump =====
TabUtility:CreateToggle({
    Name = "Infinity Jump 🤸‍♂️♾️",
    CurrentValue = false,
    Flag = "InfiniteJump",
    Callback = function(Value)
        infiniteJump = Value
    end,
})
 
-- Handler Infinite Jump
UserInputService.JumpRequest:Connect(function()
    if infiniteJump and Humanoid then
        Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
    end
end)
 
-- ===== Infinity Oxygen =====
TabUtility:CreateButton({
    Name = "Infinity Oxygen 🤿♾️",
    Callback = function()
        local ReplicatedStorage = game:GetService("ReplicatedStorage")
        local Net = ReplicatedStorage:WaitForChild("Packages")
            :WaitForChild("_Index")
            :FindFirstChild("sleitnick_net@0.2.0")
            :WaitForChild("net")
 
        local OxygenEvent = Net:FindFirstChild("URE/UpdateOxygen")
 
        local mt = getrawmetatable(game)
        setreadonly(mt, false)
        local oldNamecall = mt.__namecall
 
        mt.__namecall = function(self, ...)
            local method = getnamecallmethod()
            if self == OxygenEvent and method == "FireServer" then
                return nil
            end
            return oldNamecall(self, ...)
        end
 
        setreadonly(mt, true)
 
        Rayfield:Notify({
            Title = "Infinity Oxygen",
            Content = "Aktif ✅ (ga bisa dimatiin, rejoin untuk reset)",
            Duration = 2
        })
    end,
})
 
-- ===== Anti AFK =====
TabUtility:CreateToggle({
    Name = "Anti Turu ⏰",
    CurrentValue = true,
    Callback = function(Value)
        if Value then
            antiAfkConnection = LocalPlayer.Idled:Connect(function()
                VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
                task.wait(1)
                VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
            end)
        else
            if antiAfkConnection then
                antiAfkConnection:Disconnect()
                antiAfkConnection = nil
            end
        end
    end,
})
 
-- Section + Tutorial
TabUtility:CreateSection("📚 Tutorial 📚")
TabUtility:CreateParagraph({
    Title = "By Mammoth👑",
    Content = [[
• Kalo Mau Pake Ya Pake Aja, Paling Yang Penting Cuman Infinity Oxygen🗿
    ]]
})
